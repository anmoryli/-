<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.anmory.yunji.mapper.UserMapper">
    <update id="update" parameterType="com.anmory.yunji.entity.User">
        update user
        <set>
            <if test="username!=null">username=#{username},</if>
            <if test="passwordHash!=null">password_hash=#{passwordHash},</if>
            <if test="avatarUrl!=null">avatar_url=#{avatarUrl},</if>
            <if test="email!=null">email=#{email},</if>
            <if test="lastMenstrualDate!=null">last_menstrual_date=#{lastMenstrualDate},</if>
            <if test="pregnancyTime!=null">pregnancy_time=#{pregnancyTime},</if>
            <if test="createdAt!=null">created_at=#{createdAt},</if>
            <if test="updatedAt!=null">updated_at=#{updatedAt},</if>
            <if test="shareScope!=null">share_scope=#{shareScope},</if>
            <if test="userType!=null">user_type=#{userType},</if>
            <if test="dataCollectionEnabled!=null">data_collection_enabled=#{dataCollectionEnabled},</if>
        </set>
        where user_id=#{userId}
    </update>
</mapper>