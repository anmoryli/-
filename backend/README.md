# 需要配置的环境变量
> `IP_ADDRESS`服务器的ip地址
> 
> 关于数据库,目前连接的是我的数据库,可以自己在服务器装一个mysql然后连接自己的
> 
> 数据库用户名自己也可以配置
> 
> `MYSQL_PASSWORD`数据库密码
> 
> `OPENAI_API_KEY_PROXY`api中转站的api-key,需要花钱买,但不贵
> 这里面的模型结合spring ai是直接支持多轮对话的,所以自己训练的模型最好也能支持多轮对话
> 
> `DASHSCOPE_API_KEY`阿里云的api-key,用于向量数据库,可以去阿里云百炼平台申请