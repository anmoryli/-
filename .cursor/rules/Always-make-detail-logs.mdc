---
description: 
alwaysApply: true
---

# AI生成代码日志规范标准
## 1. 规范目的
为保障AI生成代码的可维护性、可追溯性和问题排查效率，统一所有项目（无论技术栈、规模、业务场景）的日志输出标准，确保日志能清晰反映代码执行流程、异常场景、关键节点状态，降低线上问题定位成本。

## 2. 核心原则
### 2.1 全场景覆盖
AI生成的所有可执行代码（包括但不限于业务逻辑、工具类、配置脚本、定时任务、接口层、数据访问层）必须包含标准日志，无例外场景。
### 2.2 分级清晰
日志级别划分严格遵循语义，避免级别滥用，确保不同严重程度的信息能被精准筛选。
### 2.3 信息完整
单条日志需包含定位问题的核心要素，避免“碎片化”“无意义”日志。
### 2.4 格式统一
日志输出格式、字段命名、异常记录方式全网统一，便于日志采集、解析和可视化分析。

## 3. 日志级别定义与使用规范
| 级别       | 标识       | 使用场景                                                                 | 示例                                                                 |
|---|---|-----|----|
| TRACE（跟踪） | TRACE      | 最细粒度日志，用于调试代码执行流程（如方法入参/出参、循环步骤），生产环境默认关闭 | 「TRACE」用户ID:1001，调用getUserInfo方法，入参：userId=1001          |
| DEBUG（调试） | DEBUG      | 调试关键业务流程，记录非敏感的核心步骤（如接口调用开始/结束、数据查询条件）| 「DEBUG」订单模块，调用createOrder接口，订单号：ORDER20240313001      |
| INFO（信息）  | INFO       | 记录正常业务行为，反映系统状态（如服务启动、关键操作成功、定时任务执行完成）| 「INFO」用户服务启动成功，监听端口：8080；「INFO」订单创建成功，订单号：ORDER20240313001 |
| WARN（警告）  | WARN       | 非致命异常/潜在风险，不影响核心流程但需关注（如参数不规范、缓存过期、接口超时重试） | 「WARN」用户ID:1001，手机号格式不规范：1381234567（缺失最后一位）     |
| ERROR（错误） | ERROR      | 致命异常，影响单条请求/任务执行（如接口调用失败、数据库操作异常、空指针）| 「ERROR」订单创建失败，订单号：ORDER20240313001，异常信息：SQLException: 数据库连接超时 |
| FATAL（致命） | FATAL      | 系统级故障，影响整体服务（如服务崩溃、核心依赖不可用），需立即告警           | 「FATAL」Redis连接池耗尽，无法获取连接，服务停止接收请求               |

### 3.1 级别使用禁忌
- 禁止将ERROR级别用于非异常场景（如“操作失败但可重试”的场景用WARN）；
- 禁止将INFO级别用于调试类信息（如仅开发阶段需要的临时日志用DEBUG/TRACE）；
- 生产环境默认关闭TRACE级别，DEBUG级别可按需开启（如灰度发布阶段）。

## 4. 日志必备字段
AI生成代码时，每条日志必须包含以下**基础字段**（特殊场景可补充扩展字段），字段顺序建议统一：
### 4.1 基础字段
| 字段名       | 必选 | 说明                                                                 | 示例                          |
|-----|---|----|----|
| logLevel     | 是   | 日志级别（TRACE/DEBUG/INFO/WARN/ERROR/FATAL）                        | INFO                          |
| timestamp    | 是   | 日志生成时间，格式：yyyy-MM-dd HH:mm:ss.SSS                          | 2024-03-13 15:20:30.123       |
| serviceName  | 是   | 服务名称（微服务/单体项目名称）                                       | user-service                  |
| moduleName   | 是   | 代码所属模块（如order/create、user/login、pay/callback）              | order/create                  |
| traceId      | 是   | 链路追踪ID（分布式场景）/请求ID（单体场景），全链路唯一               | 8f9e7d6c-5b4a-3s2d-1f0g-9h8j7k6l5m4n |
| content      | 是   | 日志核心内容（含业务描述、关键参数、状态）                           | 订单创建成功，订单号：ORDER20240313001 |
| exception    | 否   | 异常信息（仅ERROR/FATAL级别必填，含异常类型、堆栈、根因）             | NullPointerException: 空指针异常... |

### 4.2 扩展字段（按需添加）
| 字段名       | 适用场景               | 说明                     | 示例        |
|-----|---|-----|----|
| userId       | 用户相关操作           | 操作用户ID               | 1001        |
| requestId    | 接口请求               | 接口请求唯一标识         | req-20240313001 |
| costTime     | 性能相关               | 操作耗时（毫秒）         | 50          |
| ip           | 接口/外部交互          | 客户端/第三方IP          | 192.168.1.100 |

## 5. 各场景日志编写规范
### 5.1 接口层（Controller/API）
- 入参日志：DEBUG级别，记录请求参数、用户ID、IP、requestId；
- 出参日志：DEBUG级别，记录响应结果、耗时；
- 异常日志：ERROR级别，记录完整异常堆栈、请求上下文；
- 成功日志：INFO级别，记录接口调用成功、核心业务结果。

#### 示例（Java/SpringBoot）：
```java
@PostMapping("/order/create")
public Result<String> createOrder(@RequestBody OrderReq req, HttpServletRequest request) {
    String traceId = request.getHeader("traceId");
    String userId = req.getUserId();
    // 入参日志
    log.debug("logLevel:DEBUG, timestamp:{}, serviceName:order-service, moduleName:order/create, traceId:{}, content:创建订单入参，userId:{}, orderReq:{}", 
              LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss.SSS")),
              traceId, userId, JSON.toJSONString(req));
    try {
        long startTime = System.currentTimeMillis();
        String orderNo = orderService.createOrder(req);
        long costTime = System.currentTimeMillis() - startTime;
        // 成功日志
        log.info("logLevel:INFO, timestamp:{}, serviceName:order-service, moduleName:order/create, traceId:{}, content:创建订单成功，userId:{}, orderNo:{}, costTime:{}ms", 
                 LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss.SSS")),
                 traceId, userId, orderNo, costTime);
        // 出参日志
        log.debug("logLevel:DEBUG, timestamp:{}, serviceName:order-service, moduleName:order/create, traceId:{}, content:创建订单出参，orderNo:{}", 
                  LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss.SSS")),
                  traceId, orderNo);
        return Result.success(orderNo);
    } catch (Exception e) {
        // 异常日志
        log.error("logLevel:ERROR, timestamp:{}, serviceName:order-service, moduleName:order/create, traceId:{}, content:创建订单失败，userId:{}, orderReq:{}, exception:{}", 
                  LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss.SSS")),
                  traceId, userId, JSON.toJSONString(req), e.toString());
        return Result.fail("创建订单失败");
    }
}
```

### 5.2 业务逻辑层（Service）
- 核心步骤日志：DEBUG级别，记录业务逻辑分支、状态变更；
- 外部依赖调用日志：DEBUG级别，记录调用第三方接口/中间件的入参、出参；
- 异常日志：ERROR级别，抛至上层前记录完整异常上下文；
- 关键业务结果日志：INFO级别，记录业务规则执行结果。

### 5.3 数据访问层（DAO/Mapper/Repository）
- SQL执行日志：DEBUG级别，记录SQL语句、参数、执行耗时；
- 数据操作结果日志：DEBUG级别，记录插入/更新/删除的行数、主键；
- 数据库异常日志：ERROR级别，记录SQL、参数、异常堆栈。

### 5.4 工具类/通用组件
- 初始化日志：INFO级别，记录组件初始化状态（如Redis连接池、线程池）；
- 方法调用日志：TRACE/DEBUG级别，记录入参、出参、耗时；
- 异常日志：ERROR/WARN级别，根据异常影响范围选择级别。

### 5.5 定时任务
- 启动日志：INFO级别，记录任务名称、执行时间、参数；
- 执行步骤日志：DEBUG级别，记录任务执行进度、中间结果；
- 成功日志：INFO级别，记录任务执行完成、处理数据量、耗时；
- 失败日志：ERROR级别，记录任务执行异常、失败数据、重试状态。

## 6. 日志格式与输出要求
### 6.1 格式统一
- 文本日志：采用“键值对”格式，便于日志采集工具（如ELK、Fluentd）解析；
- 禁止使用无规则的自由文本（如：“创建订单了→用户1001→成功”）；
- 时间格式统一为：`yyyy-MM-dd HH:mm:ss.SSS`，时区统一为UTC+8。

### 6.2 敏感信息处理
- 禁止日志输出明文密码、手机号、身份证、银行卡号等敏感信息；
- 敏感字段需脱敏（如手机号：138****5678，身份证：310***********1234）。

### 6.3 异常记录要求
- ERROR/FATAL级别日志必须记录完整异常堆栈（而非仅记录异常消息）；
- 异常日志需包含“异常发生位置、触发条件、相关参数”，便于定位根因。

### 6.4 日志量控制
- 禁止在循环内输出高频日志（如百万级数据循环中每条都输出INFO日志）；
- 高频操作（如接口调用）的DEBUG/TRACE日志需按需开启，避免日志爆炸。

## 7. 技术栈适配规范
AI生成不同技术栈代码时，需遵循对应框架的日志最佳实践，同时满足上述标准：
### 7.1 Java
- 优先使用SLF4J + Logback/Log4j2，禁止使用System.out.println；
- 日志对象通过`private static final Logger log = LoggerFactory.getLogger(XXX.class);`声明。

### 7.2 Python
- 优先使用logging模块，配置Formatter统一日志格式；
- 避免使用print输出日志，日志级别通过配置文件控制。

### 7.3 Go
- 使用log/slog包，自定义Handler实现统一格式；
- 日志字段通过Key-Value方式传递，便于结构化解析。

### 7.4 前端（Node.js/JS/TS）
- 服务端日志遵循后端规范，前端控制台日志需分级（info/warn/error）；
- 接口请求日志记录请求URL、参数、响应状态、耗时。

## 8. 验收标准
AI生成代码需满足以下条件，方可视为符合日志规范：
1. 所有业务流程、核心方法均包含对应级别日志，无日志缺失场景；
2. 日志级别使用符合定义，无级别滥用；
3. 每条日志包含基础字段，异常日志包含完整堆栈；
4. 无敏感信息明文输出，日志格式统一；
5. 日志内容能清晰反映代码执行流程，可通过日志定位问题。

## 总结
1. 所有AI生成代码必须覆盖标准日志，核心是**分级清晰、字段完整、格式统一**，确保日志可追溯、可解析；
2. 日志级别需严格匹配使用场景，异常日志必须记录完整上下文和堆栈，敏感信息必须脱敏；
3. 不同技术栈、业务场景需适配统一的日志规范，同时遵循对应框架的日志最佳实践，兼顾规范性和实用性。
